name: b012_data
description: A new Flutter project.
version: 0.0.1
homepage:
repository:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.2+1
  path_provider: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: